{
  "name": "mkjs-cli",
  "version": "0.0.7",
  "description": "Make-inspired build tool",
  "main": "build/index.js",
  "directories": {
    "build": "./build"
  },
  "scripts": {
    "build": "mkdir -p build/targets && cp src/targets/*.js build/targets && jison ./src/targets/grammar.jison ./src/targets/lex.jisonlex -o build/targets/parser.js && cp src/main/* build/"
  },
  "bin": {
    "mkjs": "build/index.js",
    "mk": "build/index.js"
  },
  "repository": "blake-regalia/mkjs.js",
  "keywords": [
    "Make",
    "Makefile",
    "build-tool"
  ],
  "author": "Blake Regalia <blake.regalia@gmail.com> (blake-regalia.com)",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.2.1",
    "commander": "^2.15.1"
  }
}
