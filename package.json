{
  "name": "emk",
  "version": "0.0.1",
  "description": "ECMAScript Make - a build tool for the JavaScript elite",
  "main": "build/main.js",
  "directories": {
    "build": "./build"
  },
  "scripts": {
    "build": "mkdir -p build/targets && cp src/targets/*.js build/targets && jison ./src/targets/grammar.jison ./src/targets/lex.jisonlex -o build/targets/parser.js && cp src/main/* build/"
  },
  "bin": {
    "emk": "build/main.js"
  },
  "repository": "blake-regalia/emk.js",
  "keywords": [
    "Make",
    "Makefile",
    "build-tool"
  ],
  "author": "Blake Regalia <blake.regalia@gmail.com> (blake-regalia.com)",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.3.0",
    "commander": "^2.15.1"
  }
}
