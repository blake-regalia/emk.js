{
  "name": "emk",
  "version": "0.0.1",
  "description": "ECMAScript Make - a build tool for the JavaScript elite",
  "main": "build/main.js",
  "directories": {
    "build": "./build"
  },
  "scripts": {
    "build": "mkdir -p build/fragment && cp src/fragment/*.js build/fragment && jison ./src/fragment/grammar.jison ./src/fragment/lex.jisonlex -o build/fragment/parser.js && cp src/main/* build/"
  },
  "bin": {
    "emk": "build/main.js"
  },
  "repository": "blake-regalia/emk.js",
  "keywords": [
    "Make",
    "Makefile",
    "build-tool"
  ],
  "author": "Blake Regalia <blake.regalia@gmail.com> (blake-regalia.com)",
  "license": "ISC",
  "dependencies": {
    "bash-glob": "^2.0.0",
    "chalk": "^2.4.1",
    "yargs": "^12.0.1"
  }
}
